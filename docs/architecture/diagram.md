### HOST stands for the client who connects to the server, in this specific case, it's IP references the machine in class.

### VM-WEB stands for the virtual machine created with the sole purpose of hosting the web server.

### VM-BBDD stands for the virtual machine that manages the database and in which the database is created.

The ports relate to the usual/default ports used by the HTTPS protocol and the common Postgres database.
